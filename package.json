{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs=require('fs'); for (const file of ['package-lock.json','yarn.lock']) fs.rmSync(file,{force:true}); if (!/^pnpm\\//.test(process.env.npm_config_user_agent||'')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "dev": "pnpm --parallel --filter @workspace/api-server --filter @workspace/mobile run dev",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "dev:mobile": "pnpm --filter @workspace/mobile run dev",
    "db:push": "pnpm --filter @workspace/db run push",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "build:apk": "pnpm --filter @workspace/mobile run build:apk",
    "build:aab": "pnpm --filter @workspace/mobile run build:aab",
    "eas:login": "pnpm --filter @workspace/mobile run eas:login",
    "eas:init": "pnpm --filter @workspace/mobile run eas:init",
    "db:seed": "pnpm --filter @workspace/db run seed",
    "db:subjects": "pnpm --filter @workspace/db run subjects:sync"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
